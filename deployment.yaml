apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-deploymeny
spec: 
  replicas: 3
  # here we define the pod template
  template:
    # kind: Pod
    metadata:
      labels:
        app: api
    spec:
      containers:
        - name: api
          image: docker-web:latest

